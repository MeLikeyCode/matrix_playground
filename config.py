'''Module with program-wide (not just module-wide) global variables. Just import this module and use the global variables.'''

command_interpretter = None # will be set by by CommandInterpretter right before it executes code

LABEL_FONT = ("Arial", "20", "bold")
